# encoding: utf-8

# Brand logo, shown via the "kc-logo-text" element keycloak.v2's template header renders
# this message into. Set here (rather than relying on the realm's displayNameHtml) so the
# logo doesn't depend on that realm setting having been imported.
loginTitleHtml=<div class="kc-logo-text"><span>Ozone HIS</span></div>

poweredBy=Powered by

# Locale names
locale_km=Khmer
